# Forks environment variables (all prefixed with FORKS_)

# Path to the directory containing your recipe markdown files
# FORKS_RECIPES_DIR=/path/to/recipes

# Host and port for the backend server
# FORKS_HOST=0.0.0.0
# FORKS_PORT=8000

# Comma-separated list of allowed CORS origins.
# Defaults to localhost only when unset.
# Example for production:
# FORKS_CORS_ORIGINS=https://forks.example.com,https://www.forks.example.com
# FORKS_CORS_ORIGINS=http://localhost,http://localhost:5173,http://localhost:3000
